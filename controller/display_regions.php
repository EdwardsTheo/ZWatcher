<?php

// On récupère la région avec "GET"
// On fais le select dans la bdd where nom de région = tmp
// On renvoie avec la mise en forme dans le fichier php
// Sachant que la mise en page commune est déjà faite
// Cela permet d'afficher dans les sous-menus facilement, et de ranger précisément

    $tmp = $_GET['region'];
    // echo $tmp;
    // require('../test.php');
    echo "Section en travaux ..."



?>